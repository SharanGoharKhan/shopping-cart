import { scale } from '../../utils/scaling';

export default {
    flex: {
        flex: 1,
    },
    productCard: {
        marginLeft: '5%', 
        width: '42%', 
        height: scale(225), 
        marginTop: scale(10), 
        marginBottom: scale(20)
    }
};